# Leave blank to use browser-local edge storage (no backend required).
# Set to the Django API root (no trailing slash) once the backend is deployed.
# Example: VITE_API_BASE_URL=https://ens-network-api.onrender.com/api
VITE_API_BASE_URL=
